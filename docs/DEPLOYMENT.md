# Deployment

**Status:** Placeholder for Phase 7.

Preferred shape:

- Frontend independent deploy
- FastAPI backend container
- MCP server separate container
- Managed PostgreSQL with pgvector
- Managed Redis
- Object-storage abstraction for uploads

Will document HTTPS, secrets via environment, migrations on deploy, health/readiness, CORS, trusted hosts, non-root images, and backup notes for at least one practical platform.
